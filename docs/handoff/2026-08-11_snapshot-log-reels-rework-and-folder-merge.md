# Snapshot log — 2026-08-11 (릴스 3편 재작업·예약, 릴스 폴더 체계 통합)

> 루트 `HANDOFF.md`에서 내려온 스냅샷. 2026-08-12 세션이 교체했다.

## Current Snapshot - 2026-08-11 (릴스 3편 재작업·예약 8/25~27, 릴스 폴더 체계 통합)

- **릴스 3편이 대표님 리뷰 6건을 반영해 완성됐고 8/25·26·27 05:00 KST로 예약됐다** (FB `EpicKor` + IG `epickorsnippets`, 목록에서 6행 실측). 납품본: `output/reels/2026-08-11_{cheonggyecheon,sungnyemun,suneung}/final/`.
  - 리뷰 6건 처리: ① 수능 인트로에 **프레임 0에서 읽히는** 썸네일 카드 신설(`ONE DAY / DECIDES THE REST`) ② 수능 아웃트로 나레이션 — 잘린 게 아니라 **애초에 없었다**(7.3초 무음), 3편 전부 음성 CTA 추가 ③ 수능 교문 자료 4장 추가로 화면시간 14%→**51%** ④ 숭례문을 **현판**으로 재설계(세로 崇禮門·단청 테두리·낙관 도장) ⑤ 청계천 반복 확인 — 사실이었다, **원본 3개로 8컷**에 `31714020`이 1·4·8번에 좌우 번갈아 → **원본 7개**로 교체 ⑥ ONS 3종이 "장식만 다른 같은 뼈대"였음을 인정하고 앵커·서체·제스처를 다르게 재설계.
  - **자막은 말이 아니라 글이므로 표기를 따로 갖는다** — `epickor dot com`→`epickor.com`, 연도·측정값·큰 수치를 숫자로(16건). `scripts/reels-assemble.mjs`의 `CAPTION_FORM`이 **정렬 뒤 표시 문자열만** 치환한다.
  - 그 작업 중 **문법 단위가 끊긴 자막 3건**을 발견해 병합(2건은 같은 날 내가 쓴 아웃트로에서 생겼다). **자동 판정은 불가능하다** — 같은 규칙이 의도된 주어-반전 비트도 잡는다. 명시 목록 + 매 배치 후보 출력으로 처리.
  - **예약일이 계획과 다르다.** 배치 계획의 "8/16 gap"은 낡은 전제였고, 실측 범위는 **8/12~8/24 빈 날 0**(Korean Makers 카드뉴스가 채움). 규칙대로 8/25~27. **부작용: 릴스가 8/16~8/24 9일간 안 나간다** — 대표님 판단 대기.
- **릴스 폴더 체계를 통합했다. 한 릴스는 한 폴더다: `output/reels/YYYY-MM-DD_{slug}/`, 납품본은 그 안의 `final/`.**
  - 종전 `output/final/reels/` 트리는 **블로그 글 최종본 디렉터리**(`166_final.md` …) 안에 얹혀 있었고 그게 혼란의 원인이었다. 흡수 후 삭제.
  - **그 트리는 사본이 아니었다** — SHA로 대조하니 **120개(2.91GB)가 그쪽에만** 있었고 그중 24개는 **작업 폴더가 없는 초기 릴스 13편(170~184)의 유일한 사본**이었다. gitignore라 git 복구도 불가능하다. 그래서 삭제가 아니라 **이동 후 빈 트리 제거**(139개 이동, 중복 19개로 288MB 회수, 손실 0).
  - 87폴더에 날짜를 붙였고 날짜는 **파일 mtime**에서 왔다 — `output/reels/**`가 대부분 untracked였던 탓에 git은 거의 전 폴더를 오늘로 보고한다. 스크립트에는 계속 **맨 슬러그**를 넘기고 `scripts/lib/reel-dir.mjs`의 `reelFolder()`가 푼다(없으면 오늘 날짜 생성). **`public/assets/reels/`는 런타임 경로라 의도적으로 날짜를 안 붙였다.**
- **`schedule-meta-reel.py`에 오전/오후 버그가 있었다** — FACTS에 카드뉴스용으로 기록된 "`aria-valuetext`로 읽어라"가 릴스 스크립트엔 반영돼 있지 않았다. `input_value()`는 **항상 빈 문자열**이라 조건이 영영 참이 안 되고 ArrowUp을 최대 횟수 눌러 착지한다. 고치고, **커밋 직전에 두 행의 날짜·오전/오후·시·분을 전수 되읽어 하나라도 어긋나면 클릭 거부**를 추가했다.
- 주요 커밋: `140654a8`(자막·폴더 날짜) `0e6316cf`(AGENTS 문서) `919fb891`(트리 통합) `dc4ab939`(예약 스크립트).
- **다음**: ① **대표님 폰 검토(소리 켜고/끄고)** — 8/25까지 시간 있음, 문제 시 예약 수정·철회 가능 ② 릴스 지표 — 8/13·14·15 게시분을 `output/reels/metrics.json`에 기록(저장소에 실측 조회 데이터가 아직 0건) ③ 다음 GSC 추출로 리프레시 45편 효과 + 2차 레인 재판정.
