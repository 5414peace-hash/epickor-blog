# Snapshot log — 2026-08-19~20

루트 `HANDOFF.md`에서 2026-08-21에 내려온 직전 Current Snapshot 본문이다.
당시 살아 있던 항목(`071` 판정일, 리프레시 큐 상태, 날짜 작업)은 루트에 압축해 남겨둔다.

- **하루 리듬 완주: 신규 2편(`417`·`418`) + 리프레시 1편(`167`). 전부 라이브 200 확인.**
  - `417` 꼬깔콘 — **큐에 적힌 각도를 조사 중에 바꿨다.** 189는 "손가락에 끼워 먹는 과자"였는데
    롯데가 야장시리즈 4종을 **편의점별로 쪼개 팔아** "편의점 투어"가 생긴 것(15일 80만 봉)이
    훨씬 강한 행동 프레임이었다. 영어권 커버리지 0건.
  - `418` 식품값 인상 — 4개사 3주, **파리바게뜨 8/25는 아직 안 왔다**(행동 가능). 핵심은
    파리바게뜨가 밝힌 사유가 드러낸 구조다: **억눌린 인상은 사라지지 않고 줄 서 있다가 함께 풀린다.**
  - **큐 152(델리만주 레시피)는 기각**했다 — 071이 이미 그 의도를 답하고 `delimanjoo recipe` 2.6위다.
  - 이미지 5장 중 4장이 자체 도표. **"어느 편의점이 어느 맛인가"는 사진이 못 나르는 정보**다.

- **`071` 델리만주 내부링크 3 → 11개 (커밋 `19133510`, 라이브 8편 전수 검증됨).**
  사이트 최대 단일 순위 기회. **판정은 9/23, 기준은 순위이지 CTR이 아니고, 오타 쿼리로 재면 안 된다** —
  베이스라인·개입 내역·판정 함정 전부 `output/strategy/071-internal-link-experiment.md`.
  링크마다 **서로 다른 문장으로 실제 정보를 추가**했다(명동역 1998년 1호점·슈크림 붕어빵 관계·
  역 간식 가격 등) — 링크만 끼우지 않았다. 9편 리뷰어 100/100.

- **착수 전 검증으로 내 오진 1건을 잡았다: `074` 제목 리프레임 추천은 폐기됐다.**
  그 페이지 노출의 **68.7%가 따옴표 연산자 쿼리**(순위추적 봇)였고, "4위·0클릭"은 봇 아티팩트였다.
  게다가 본문에 이미 홍대 답(AK플라자 4번 출구)이 전용 섹션으로 있었다.
  **사이트 전체 오염은 1.0%, 그중 94%가 074 한 페이지** — 다른 결론은 무사하다.
  규칙: **"높은 노출 + 좋은 순위 + 0클릭"은 기회이기 전에 봇일 수 있다.**

- **리프레시 큐가 수명을 다했다 (다음 세션이 결정할 것).** 31편 남았는데 **전부 tier 3**이고,
  큐가 **GSC 순위를 안 본다** — 스펙 결손으로만 뽑아서 `071`·`181`·`043`처럼 4~9위에 노출이
  대량으로 걸린 페이지가 큐에 아예 없다. 순위 신호를 넣지 않으면 계속 저가치 작업만 나온다.

- **GSC API 직접 접근 확보 (대표님 지시).** blog-news의 OAuth 토큰이 epickor 속성을 siteOwner로
  커버한다 — `secrets/`(gitignored)에 복사, 추출은 **`node scripts/gsc-pull.mjs`** (raw fetch;
  googleapis 버전은 이 환경에서 행). UI 내보내기·서비스 계정 설정 둘 다 불필요해졌다.
  전수 재검증 결과는 `output/strategy/gsc-review_2026-08-20.md` 부록 11~17절:
  ① 코호트 결론 유지(신규가 중앙값 CTR·순위 모두 최고) ② **클릭의 77%는 익명화 초롱테일**(쿼리
  차원으로 재면 1/4 오독) ③ **노출 11%가 자동번역 SERP, 33%가 인도·인니·필리핀**(KPI에서 제외하고
  읽을 것; 한국·싱가포르가 CTR 1~2위) ④ 순위 푸시 1순위 = **071 델리만주**(4~9위 노출 8,259),
  **074는 제목이 클릭을 걷어참**(`hongdae underground mall` 4위·0클릭 — "Isn't One" 프레임)
  ⑤ 402~416 **15편 전수 색인 완료**(발행 1~2일 내).

- **GEO/SEO 점검(대표님 지시, 헤일로X 체크리스트 기준) 완료 — 커밋 `d2db970b`, 라이브 검증됨.**
  최대 발견: **FAQ 스키마가 아카이브의 70%(240여 편)에서 조용히 안 나가고 있었다** — 추출기가
  최신 FAQ 포맷만 읽어서. 세 포맷 모두 읽게 고쳐 **FAQPage 67→346/383편**. 그 외 `/studio`·`/preview`
  noindex+robots 차단, 홈 h1·canonical·Organization(sameAs) 스키마, `/llms.txt` 신설.
  SSR·AI봇 접근·기사 스키마·허브 canonical은 점검 결과 이미 정상이었다. 상세는 FACTS `gsc / strategy`.

- **유튜브 쇼츠 10편 추가 예약 완료 — 08-30~09-08.** 매니페스트 `output/youtube/bridge-2026-08-30-manifest.json`
  (영상 ID·검증 기록 포함). 목록 재확인: **08-20~09-08 20일 연속 전부 `예약됨`, 초안 0.**
  선정은 08-14 배치 실측 기준(넓은 호기심 / 보편적 불안 해소)을 따랐다 — 음식 5, 문화충격 3,
  교육 1, 도시사 1. 여행 로지스틱스(찜질방 263)와 쇼핑 니치(MU:DS 138)는 제외.
  `376` 1+1편만은 쇼핑 니치에 가까워 **이 배치에서 가장 위험한 픽으로 매니페스트에 명시**했다.
- **인스타는 이미 하루 1건이 돌고 있었다 — 다만 카드뉴스만이 아니라 릴스와 슬롯을 나눠 쓰고 있다.**
  `/latest/posts/scheduled_posts` 실측: **08-21~09-17 28일 연속, 빈 날 0.**
  `CARDNEWS_INDEX.md`만 보면 8/25~9/2·9/6~9/8·9/12가 비어 보이는데 **전부 릴스가 채우고 있다.**
  하마터면 "13일 공백, 카드뉴스 13편 신규 제작 필요"라는 틀린 결론을 낼 뻔했다.
- **그 과정에서 실제 결함 1건을 찾아 고쳤다.** `musinsa` 카드뉴스가 인덱스에는 09-17로 적혀 있는데
  플래너 실제 값은 **09-03 오후 8:00**이었고, 같은 날 05:00의 `395`와 겹쳐 하루 2건이 나가고
  09-17은 비어 있었다. FB·IG 두 행 모두 09-17 05:00으로 옮기고 전체 재스캔 —
  **28일 연속·중복 0·빈 날 0.** 인덱스는 *의도한 날짜*를 적을 뿐 예약을 재확인하지 않는 구조다.
- **통합 캘린더를 `docs/social-calendar.md`로 남겼다.** 이 착시가 재발하지 않게 하는 게 목적이다.
- **재고 상태가 두 채널이 다르다.** 인스타는 **카드뉴스 73폴더·릴스 59렌더 전량 예약 완료 — 09-17
  이후는 신규 제작이 필요**하다. 유튜브는 **미게시 릴스가 약 42편** 남아 09-08 이후는 스케줄링만 하면 된다.
- 자동화 실측 2건(FACTS 기록): `connect_over_cdp`는 **50MB 초과 파일을 못 넣어** 허브 렌더(68~100MB)가
  막혔고, **CDP `DOM.setFileInputFiles`**로 우회했다. 스튜디오 날짜 선택기는 `ytcp-scrollable-calendar`라
  **8·9·10월이 한 화면에 쌓여 있어** 월 이동이 아니라 **월 라벨 y구간 안의 날짜 셀**을 클릭해야 한다.
- **코레일 추석 예매 일정이 확정돼 `200`·`380`을 정정·배포·라이브 검증했다 (커밋 `09883d97`).**
  공지는 **8/13에 등록**돼 있었다 — 우리가 확인하고 "미발표"로 적은 바로 그날이다.
  **널리 퍼진 "8/20 KTX · 8/25 SRT"는 틀렸고**, 실제는 사전예매 **9/3~9/4**, 일반예매 **9/7~9/11**(노선별),
  잔여석 **9/11 15:00~**(역 창구는 이때부터). `200`은 **제목에 "late August"가 박혀 있어** 독자를
  한 달 틀린 시점으로 보내고 있었다 — 제목·description·표·FAQ 전면 교체, 둘 다 리뷰어 100/100.
  - **380이 던져놓은 열린 질문의 답이 나왔다.** 합병 후 SR 별도 판매가 사라지고
    **수서 출·도착 KTX가 9/10 하루를 따로 갖는다** — 서울~부산(9/11)보다 하루 빠르다.
  - **교훈: "오늘 확인했더니 공지가 없다"는 사실은 하루짜리다.** 추정 수치를 제목·description처럼
    되돌리기 비싼 자리에 넣지 않고, 재확인 날짜를 캘린더에 박는다.
- **GSC 8/20 추출 분석 완료 (`output/strategy/gsc-review_2026-08-20.md`).** 주간 클릭 5~6월 74~118 →
  8월 둘째 주 **243, 3배 성장**. 분기 최대 승자는 리프레시(198 워터밤 +98클릭·5.4%).
  - **제목 게이트를 만들려다 기각했다.** 집계로는 1.7배였는데 중앙값이 0.61 대 0.60으로 동일했고,
    우위를 만든 상위 3편이 전부 리프레시분이었다. **2026-07-31 "장소+결정 2.8배"와 같은 유형** —
    이번엔 게이트를 넣기 전에 잡았다. 재시도 금지, 사유는 FACTS에 있다.
  - **실제 기제는 순위다.** 1~3위 1.53% / 5~11위 0.14%, **노출의 87%가 5위 밖.**
    순위를 통제하면 롱테일이 헤드를 **2.4배**(0.33% vs 0.14%, 중앙순위 8.0 대 8.2) — 뾰족함 전략의
    이상치 없는 증명.
  - **챕터1 게이트 "집계 CTR ≥1.5%"가 잘못된 지표였다.** 사이트에서 1.5%를 넘는 건 1~3위 구간뿐.
    **중앙값으로 바꿨다**(CLAUDE.md 반영) — 그 기준으로는 신규 코호트가 1.08%로 **가장 좋다**.
  - 리뷰어에 **스펙 v1 경고 추가**(₩ 가격·한글). 세 지표 모두에서 일관된 유일한 본문 속성.
- 다음 날짜 작업: **8/24 쇼츠 판정 + Studio 트래픽 소스에서 epickor.com 유입 확인**,
  **9/6~9/8 DOSSIER 완주율**, **9/23 GSC 재추출**. 추석 관련으로는 **9월 초 궁궐 무료개방 발표**가
  아직 미확정이라 `200`이 "미발표"로 열어두고 있다 — 그때 재확인한다.
