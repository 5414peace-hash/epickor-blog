# HANDOFF archive - 2026-07-16 snapshot (Blogs 291-302, Samyang business post, image-quota fix)

Moved out of the root `HANDOFF.md` on 2026-07-20 to keep the fast-start dashboard under 250 lines.
All work below was completed, deployed, and publicly verified at the time of writing.

- Blogs `301` and `302` are published and publicly verified: `Ajumma Meaning` and `What Koreans Eat for Breakfast`. Commit `da105966` is on `origin/master`; Vercel deployment `epickor-blog-phgvp0ssf` is Ready and aliased to `www.epickor.com`. Both scored 100/100; the 351-page production build passed. Local desktop/mobile and public 390px Edge QA confirmed all eight article images decoded, two tables and two affiliate CTAs per post, table-only mobile overflow, and no document-level overflow. Both pages, all eight assets, sitemap entries, and reverse links from Blogs `090`, `210`, `153`, and `171` returned public HTTP 200 with expected content.

- Latest normal posts: Blogs `298`-`300` — Seoul stationery shopping, Korean cookware starter kit, and Seoul museum gift shops. Commit `1f6d6852` is on `origin/master`; Vercel deployment `epickor-blog-ncvkodalc` is Ready and aliased to `www.epickor.com`.
- Public URLs `https://www.epickor.com/blog/298`, `/299`, and `/300` returned HTTP 200 with expected titles; all 12 image assets, three sitemap entries, and reverse links from Blogs `293`, `287`, and `272` passed. Each post has four directly matched images, two rendered HTML tables, two disclosed Amazon affiliate CTAs, six FAQs, and official source links.
- Automated post review is 100/100 for all three. The final production build generated 349 static pages. Full-page desktop, local 390px, and post-deploy 390px Edge renders confirmed decoded images, readable CTA boxes, table-only horizontal scrolling, and no document-level overflow. Blog 300's three low-resolution official National Museum images were display-upscaled to 1200px after the first render showed undersized inline images.
- Latest normal posts: Blogs `294`-`297` — Korean metal chopsticks/sujeo, Korean temple food, Seoul hanok experiences, and Korean traditional desserts.
- Public URLs: `https://www.epickor.com/blog/294`, `/295`, `/296`, and `/297`; all verified HTTP 200 with expected titles, two rendered tables, two affiliate CTAs, and sitemap entries.
- Automated SEO review remains 100/100 for all four. After the representative image-fit correction, 294 has 3 images, 295 has 4, and 297 has 5; all retain two HTML tables, two affiliate CTAs, and six FAQs.
- Image correction QA: 294 now centers the sujeo set and flat metal chopsticks; 295 uses only temple food/course/cooking visuals; 297 directly matches assorted tteok, injeolmi, yakgwa, and sikhye. All 12 replacement assets returned public HTTP 200 and were visually checked on desktop and 390px responsive layouts.
- Production build generated 346 static pages. Vercel deployment `epickor-blog-64zapwe1k` is Ready and aliased to `www.epickor.com`.
- Deployment commits: `310ccf4a` finalizes the six card-news captions; `0d1477cb` publishes Blogs 294-297; `f7e5ccb6` applies the representative-directed image correction to 294/295/297.

- Latest normal posts: Blogs `291`-`293` — K-pop Photocard, Korean Coin Noraebang, and Seoul Bookstore/Library guides.
- Public URLs: `https://www.epickor.com/blog/291`, `/292`, and `/293`; all verified HTTP 200 with the expected title.
- Blogs 291-293 contain 2,233-2,360 review-counted words, four images, two HTML tables, two affiliate CTAs, and six FAQs each.
- Automated SEO review: 100/100 for all three.
- Full production build: 342 static pages generated successfully.
- Local rendered QA: all 12 article images loaded with nonzero dimensions; all six tables and six affiliate CTAs rendered; no page-level horizontal overflow.
- Image package: official KTO/Seoul sources, one Pexels Starfield image, and six generated illustrative images with real artists/brands excluded. Generated PNGs were optimized to 156-225 KB JPEGs.
- Three reverse links were added from Blogs 193, 170, and 251.
- Production commit `933c9019` is on `origin/master`; Vercel deployment `epickor-blog-3qmmu5fiw` is Ready and aliased to `www.epickor.com`.
- Public QA verified 12/12 image assets HTTP 200, all three sitemap entries, and reverse links from Blogs 193, 170, and 251.

- Sitewide thumbnail outage diagnosed on 2026-07-12: original assets returned HTTP 200, while Vercel `/_next/image` returned HTTP 402 `OPTIMIZED_IMAGE_REQUEST_PAYMENT_REQUIRED` after the image-transformation quota was exhausted.
- `next.config.ts` now sets `images.unoptimized: true`, so all `next/image` surfaces use direct source URLs instead of the failing optimizer. Production deployment `epickor-blog-4otrp4yvf` is Ready and aliased to `www.epickor.com`; public mobile checks confirmed the Blog 291 hero and the first two Latest thumbnails loaded at nonzero natural dimensions from direct asset URLs. (This is the reason body-image file size is now a hard operating concern: originals are served to users as-is. See the image-weight rules in `CLAUDE.md`.)

- Latest Business post is the published Samyang Foods B-2 deep dive: `https://www.epickor.com/business/samyang-foods-buldak-global-deep-dive`. Commit `780a32b9` is on `origin/master`; Vercel deployment `dpl_HWFzLMa8Z2aighYhzCkiWviTJeyv` is Ready and aliased to `www.epickor.com`.
- The article has six official/EpicKor visuals, three HTML tables, two disclosed Amazon CTAs, six FAQs, and official company/IR sources. The 352-page production build passed. Local and post-deploy desktop Edge renders were visually inspected; the public article, six assets, sitemap entry, and reverse links from Blogs `219` and `277` returned HTTP 200 with expected content.
- Card-news batches `081/288/290` and `036/170/287` remain representative-confirmed and ready for scheduling. Final English Instagram captions, short alternatives, posting notes, and upload-ready `caption.txt` files are synchronized between `output/cardnews` and `public/assets/cardnews`.
- `reports/seo-aeo-audit.md` is now regenerated and committed with each audit run; no unrelated dirty files remain.
