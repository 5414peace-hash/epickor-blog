# Snapshot archive — 2026-08-14

> 2026-08-15 세션에서 루트 HANDOFF의 Current Snapshot 자리를 비우며 내려온 기록.

## Current Snapshot - 2026-08-14 (유튜브 첫 실측 + 커뮤니티 카드뉴스 개시 + 릴스 디자인 토큰화 + 서울 쇼핑 코너)

- **유튜브 커뮤니티에 카드뉴스 배포를 시작했다. 여기가 링크가 눌리는 유일한 자리다.** `311`(떡볶이) 게시 완료, **8/15~8/25 11편 예약 완료**(313·171·218·312·257·220·192·259·184·288·242, 매일 23:00, 빈 날 0). 인스타 캡션도 일반 쇼츠 설명란도 URL이 안 눌리는데 커뮤니티는 눌린다 — 퍼널의 첫 다리가 여기서 놓인다. 쇼츠가 8/23까지 매일 21:00이라 **8/23까지 하루 2개**가 채워진다. 배치 `yt_schedule_batch.py`, 검증 `yt_verify_scheduled.py`.
- **릴스 디자인을 2층 토큰으로 분리했다(대표님 지시).** 코어(`remotion/tokens/core.ts` — 안전영역·타입스케일·캡션 거동·모션)와 테마(`theme.ts` — 영상마다 새로 정하는 색·배경·판·폰트 페어링). **폰트 5벌을 프로젝트에 번들**(Inter/Archivo/Oswald/Newsreader + Noto Sans KR 124서브셋, `public/assets/fonts/`) — 시스템 폰트 의존을 끊었다. **팔레트는 감이 아니라 그 영상 푸티지에서 뽑는다**: `scripts/extract-palette.mjs`가 컷 플레이트를 k-means로 갈라 최대 면적 → 캔버스(채도 유지), 색조 40° 이상 벌어진 두 개 → 액센트, 가장 빈 색조 → 하이라이트, 휘도/분산 → 캡션 판 스타일을 정한다.
- **그러나 시안 4개는 임팩트가 없었고 대표님 지적이 맞았다. 실측: A vs C가 픽셀 0.4%, A vs D가 4.7% 차이다.** 사진이 화면을 덮고 골격이 동일해서 테마가 건드리는 건 칩·타일·자막판뿐이다. **얕은 층(색·폰트)으로는 릴스 임팩트를 못 만든다.** 더 근본적으로 EpicKor는 영상을 안 찍으므로 릴스는 구조적 상한이 있고, **카드뉴스는 그 핸디캡이 없다** — 무게 중심을 카드뉴스로 옮기기로 했다.
- **릴스 결함 3종 수정 + 컷 경계 ONS 겹침 발견·수정.** 379 첫 프레임 검정(평균휘도 16.2 → **50.8**, 밝은픽셀 0.2% → 31.7%), 캡션 페이드/이동 제거(3편), 상단 컷존 침범 제거(377 y0-150 엣지 2,139px → **0**). 그리고 **네 키트 전부에서 `TextGate` 퇴장 페이드가 다음 컷 시작 후 8프레임 더 흘러 두 마스트헤드가 겹쳤다** — 377 f203이 `THE TIMING | 음주 전 | CE STORE | 숙취해소제`로 판독 불가였다. 페이드가 경계에서 끝나도록 수정.
- **신규 5편 발행, 전부 라이브 검증.** 8/13 `380`(KTX·SRT 통합)·`381`(스팸 선물세트)·`382`(사이렌 vs 재난문자), 8/14 `383`(서울 캐리어)·`384`(광장시장 구제상가), 그리고 비즈니스 `navien-tankless-water-heater-deep-dive`.

- **신규 5편 발행, 전부 라이브 검증.** 8/13 `380`(KTX·SRT 통합)·`381`(스팸 선물세트)·`382`(사이렌 vs 재난문자), 8/14 `383`(서울 캐리어)·`384`(광장시장 구제상가), 그리고 비즈니스 `navien-tankless-water-heater-deep-dive`.
- **유튜브를 처음으로 실측했다. 인스타와 정반대로 작동한다.** @EpicKor 구독 510, 쇼츠 30편, 누적 42,933뷰. **같은 DMZ 릴스가 인스타 2,172뷰/저장27인데 유튜브 203뷰다.** 7~8월 배치(나이 동일) 하위 3개가 전부 쇼핑·활동·서비스 니치(MU:DS 138·헤어살롱 138·러닝 200)이고 상위 4개가 전부 넓은 문화 호기심 또는 보편적 불안 해소(노래방 1,668·파전 1,605·공중화장실 1,506·사회규칙 1,392). **인스타 저장수로 유튜브 업로드를 고르면 안 된다** — 그 기준으로 짠 1순위 목록을 폐기하고 다시 짰다. 원장 `output/reels/metrics.json`, 스터디 `output/youtube/UPLOAD_STUDY_2026-08-14.md`.
- **쇼츠 10편 예약 완료 (8/14~8/23 매일 21:00).** 176 찜질방 → 294 금속젓가락 → 186 혼밥 → 187 미신 → 326 매운맛 → 182 직장문화 → 174 지하철 → 301 아줌마 → 181 웹툰 → 190 의료. 목록에서 독립 재검증(예약됨 10건, 빈 날 0). 매니페스트 `output/youtube/tier1-2026-08-14-manifest.json`.
- **서울 쇼핑 코너 착수(대표님 승인).** 신설이 아니라 **방치된 자산**이었다 — 기존 54편이 이미 클릭 239/노출 23,481/**CTR 1.018%**로 사이트 평균(0.390%)의 2.6배다. 운영서 `section_seoul-shopping-playbook.md`, 기준선 원장 `section_seoul-shopping-baseline.json`. **`where to buy`를 즉시 기각하던 플레이북 §2 규칙이 우리 1위 글(`275`, 9.27%)을 죽일 뻔해 예외를 명시했다** — 범위가 다르다(`where to buy buldak`은 아마존이 먹지만 `...in Seoul`은 목적지 쿼리다).
- **아마존 재신청 계정에 실판매 1건 발생** (커미션 $2 미만). 3건 중 1건, 기한 **2027-01-23**. "전환은 되는데 클릭이 병목"이라는 진단이 유효하다.
- **비즈니스 섹션 5일 만에 재가동.** 파세코를 1순위로 올렸다가 **SERP 실사에서 기각**하고 나비엔으로 교체·발행했다. 후보 문서 `business-candidates_2026-08-14.md`.
