# 071 델리만주 — 내부링크 실험 (착수 2026-08-20)

## 왜 이 페이지인가

GSC API 전수 실측(창 2026-05-18~08-18)에서 **사이트 최대의 단일 순위 기회**로 확인됐다.
오염(따옴표 연산자 쿼리) **0%**. 근거는 `gsc-review_2026-08-20.md` 19절.

핵심은 **철자별 성적 분기**다. 우리는 3~5위에 있을 때 2.4~2.9%로 전환하는데,
사람들이 가장 많이 치는 정확한 철자 두 개에서만 7위에 있다.

## 베이스라인 (2026-05-18 ~ 2026-08-18, 93일)

페이지 합계: **노출 9,652 / 클릭 84 / 104쿼리**

| 쿼리 | 노출 | 클릭 | CTR | 순위 |
|---|---|---|---|---|
| `deli manjoo` | 3,336 | 12 | 0.36% | **7.0** |
| `delimanjoo` | 2,564 | 7 | 0.27% | **7.5** |
| `duli manjoo` (오타) | 550 | 15 | 2.73% | 3.0 |
| `doli manjoo` (오타) | 544 | 13 | 2.39% | 4.7 |
| `doli manjoos` (오타) | 376 | 11 | 2.93% | 4.6 |
| `manjoo korean snack` | 212 | 2 | 0.94% | 4.1 |
| `deli manjoo myeongdong` | 74 | 0 | 0% | 9.8 |

**내부 인바운드 링크: 3개** (`153`·`220`·`280`) — 대조군 `171`은 25개.

## 개입 (2026-08-20)

인바운드 **3 → 11개**. 각 글의 문맥에 맞는 서로 다른 문장으로 삽입했고,
전부 독자에게 실제로 쓸모 있는 정보를 추가한다 (링크만 끼워넣지 않았다).

| 글 | 문맥 | 추가한 정보 |
|---|---|---|
| `315` 명동 | 길거리 음식 가격 사다리 | **1998년 명동역 4호선 1호점** + 1인 ₩3,000~5,000이 길거리 ₩4,000~8,000보다 쌈 |
| `416` 붕어빵 | 팥붕/슈붕 논쟁 | 슈크림파에게: 같은 커스터드를 사철·지하에서 파는 것 |
| `171` 편의점 아침 | 출근 아침 (사이트 클릭 3위 글) | 편의점 밖의 통근 간식, 1998년부터 같은 아침 인파 상대 |
| `278` 커피믹스 | 페어링 목록 | 이미 이름만 있던 것을 링크로 |
| `323` 한국에서만 | 재현 불가 음식 | 왜 길거리 음식이 아니라 프랜차이즈인지 |
| `288` 포장마차 | 포차의 정의 | 역방향 사례 — 길거리 음식처럼 느껴지지만 텐트에 없음 |
| `255` 백화점 푸드홀 | 기념품 주의사항 | 못 가져가는 것 목록에 추가 |
| `059` 편의점 음식 | 2026 가격 벤치마크 | 도시락이 더는 최저가가 아니라는 문단에 역 간식 가격 |

앵커 다양성: `Deli Manjoo (델리만주)` ×3 / `Deli Manjoo guide` ×4 / `Deli Manjoo` ×2 / `delimanjoo` ×2.
**`delimanjoo` 붙여쓰기 앵커를 일부러 넣었다** — 그 쿼리가 2,564노출 7.5위이기 때문이다.

아웃바운드도 보강: `071` → `315`(명동 가격 맥락), `071` → `416`(형제 간식).

## 판정 (2026-09-23 GSC 재추출 때)

```bash
node scripts/gsc-pull.mjs --dimensions query,page --start 2026-08-21 --end 2026-09-21
```

**성공 기준**: `deli manjoo`·`delimanjoo`의 **순위**가 7.0~7.5 → 5위권 이내로 이동.
CTR은 순위를 따라오므로 순위를 먼저 본다.

**주의 — 판정 함정 셋:**
1. **오타 쿼리(`duli`/`doli`)로 판정하지 말 것.** 이미 3~5위라 개선 여지가 없고, 전체를 좋아 보이게 만든다.
2. **CTR 집계로 판정하지 말 것.** 2026-08-20 규칙 — 쿼리별 순위로 본다.
3. **효과가 없으면 없다고 기록할 것.** 내부링크는 순위 개입 중 가장 약한 수단이고,
   `deli manjoo` SERP에는 공식 프랜차이즈·위키·대형 매체가 있다. 실패도 유용한 결과다 —
   그러면 다음 수단(외부 언급·콘텐츠 심화)으로 넘어가야 한다는 뜻이다.
